# Markdownで歌詞を書いたら自動でMVができた話

*2026-03-08 / AICU Inc.*

「歌詞をMarkdownで書くだけでリリックビデオが作れたら最高じゃない？」

そんな思いつきから始まったプロジェクト **music-slide-maker** の初回ビルドから、実際の楽曲でBGM付きリリックビデオを完成させるまでの記録です。

---

## このプロジェクトについて

**music-slide-maker**（正式名: `marp-lyric-video`）は、Marp形式のMarkdownで歌詞を書くと、[Remotion](https://www.remotion.dev/)（Reactベースの動画生成フレームワーク）で自動的にリリックビデオ（MP4）を生成するツールです。

技術スタック:
- **Remotion** — Reactで動画を作るフレームワーク
- **Marp** — MarkdownをスライドにするパーサーおよびCLI
- **TypeScript / React**
- **aubio** — 音声分析ライブラリ（BPM検出・ビートトラッキング）

---

## Step 1: まずはビルドしてみる

```bash
npm install  # 301パッケージ、脆弱性0件
npm run build
```

いきなりエラー。`package.json`のスクリプトが`src/index.ts`を参照していたが、ソースファイルはすべてルート直下にあった。パスを修正したら無事ビルド成功。

```
⚡️ Cached bundle. Subsequent renders will be faster.
```

## Step 2: サンプル動画をレンダリング

```bash
npm run render
```

360フレーム（12秒 @ 30fps）のサンプル動画が生成された。Remotionの初回レンダリングではChrome Headless Shellが自動ダウンロードされる。並列5xで数秒で完了。

`npm run studio` でRemotion Studioも起動。ブラウザ上でリアルタイムプレビューしながらアニメーションやタイミングを調整できる。

## Step 3: 歌詞フォーマットを拡張する

Marp Markdownの歌詞フォーマットを拡張して、スライドごとにアニメーション種類を指定できるようにした。

```markdown
<!-- duration: 3, animation: slide -->
君の声が 風に乗って
```

1つのHTMLコメントで**表示時間**と**アニメーション種類**（fade / slide / scale / typewriter）を同時に指定できる。`marp-parser.ts`のパーサーを修正して対応。

## Step 4: 実楽曲でリリックビデオを作る

ここからが本番。実際の楽曲 **「PicoPico! Shooting Game 123」**（Mina Azure / #PPSG #AiCuty）でリリックビデオを作る。

### MP3の取得

```bash
brew install yt-dlp
yt-dlp -x --audio-format mp3 --audio-quality 0 \
  -o "public/ppsg123.mp3" \
  "https://www.youtube.com/watch?v=5KiCKixsmr0"
```

105秒（1分45秒）のMP3が`public/ppsg123.mp3`に保存された。

### 歌詞をMarkdownに

34フレーズの歌詞をタイミング付きのMarp Markdownに変換。しかしここで問題が——手動でタイミングを振ると楽曲とズレる。

## Step 5: 音声分析でタイミングを同期する

歌詞と楽曲を正確に合わせるため、**音声分析**を導入した。

### BPM検出

```bash
brew install aubio
aubiotrack public/ppsg123.mp3 > beats.txt
```

aubioのビートトラッキングで**188個のビートポイント**を検出。ビート間隔の中央値から**BPM 121**を算出。

| パラメータ | 値 |
|-----------|-----|
| BPM | 121 |
| 1拍 | 0.496秒 |
| 1小節（4拍） | 1.983秒 |
| 最初のビート | 1.606秒 |

### YouTube自動字幕との照合

```bash
yt-dlp --write-auto-sub --sub-lang ja-orig --sub-format srt \
  --skip-download "https://www.youtube.com/watch?v=5KiCKixsmr0"
```

自動生成字幕（22エントリ）のタイムスタンプを取得し、小節番号にマッピング:

```
字幕  4.40s → 小節 2.4（Verse 1 開始）
字幕 37.72s → 小節19.2（Chorus 開始）
字幕 72.64s → 小節36.8（Bridge付近）
```

### 小節単位のタイミング配置

ビート分析と字幕を統合して、`fromBar(n)` 関数で全34フレーズを小節単位で配置:

```typescript
const fromBar = (n: number) => b1 + (n - 1) * bar;

const timings = [
  [0.0, fromBar(2), "PicoPico! Shooting Game 123", "scale"],
  [fromBar(2), fromBar(4), "毎日 タスクまみれ", "fade"],
  // ...拍に乗った正確なタイミング
];
```

## Step 6: 歌詞スタイルの調整

リリックビデオらしいスタイルに変更:

- **位置**: 画面中央下部（字幕風）
- **文字色**: 赤（`#ff1a1a`）
- **縁取り**: 白フチ（`WebkitTextStroke: 3px white`）
- **フォント**: ゴシック体（`Hiragino Kaku Gothic ProN`）

```tsx
// LyricSlide.tsx
<div style={{
  color: textColor,
  WebkitTextStroke: "3px white",
  paintOrder: "stroke fill",
  // ...
}}>
```

### バグ修正

短いスライド（アウトロの8フレーム等）でフェードイン/アウトの区間が重複し、Remotionの`interpolate`が`inputRange must be strictly monotonically increasing`エラーでクラッシュする問題を発見・修正:

```typescript
// 修正前: fps * 0.3 が durationInFrames を超えるケース
const fadeInDuration = fps * 0.3;

// 修正後: スライド長の1/3を上限に
const maxFade = Math.min(fps * 0.3, durationInFrames / 3);
```

## Step 7: Marpスライド（PowerPoint形式）生成

歌詞をプレゼン資料としても使えるよう、Marp CLIでPowerPointとHTMLスライドを生成:

```bash
npx @marp-team/marp-cli --no-stdin ppsg123-slides.md --pptx -o out/ppsg123-slides.pptx
npx @marp-team/marp-cli --no-stdin ppsg123-slides.md --html -o out/ppsg123-slides.html
```

Verse / Chorus / Bridge ごとに異なるデザイン（グラデーション背景、ネオン風タイトル）を適用した16スライド構成。

---

## 成果物

| ファイル | 形式 | サイズ | 内容 |
|---------|------|--------|------|
| `out/video.mp4` | MP4 | 7.8 MB | BGM付きリリックビデオ（105秒） |
| `out/ppsg123-slides.pptx` | PPTX | 5.7 MB | PowerPointプレゼン（16スライド） |
| `out/ppsg123-slides.html` | HTML | 123 KB | ブラウザプレゼン |

## 学んだこと

1. **Remotion + Marp** の組み合わせは、Markdownだけでリリックビデオを作れる強力なワークフロー
2. **aubio** を使った音声分析は、手動タイミング調整よりも遥かに正確で再現性がある
3. YouTube自動字幕は精度は低いが、**タイミング情報**としてはビート分析との照合に使える
4. `interpolate` のinputRangeは必ず単調増加でなければならない——短いスライドのエッジケースに注意

## リポジトリ

- GitHub: [aicuai/music-slide-maker](https://github.com/aicuai/music-slide-maker)
- Issue: [#1 PPSG123: 楽曲分析によるタイミング同期 + Marpスライド生成](https://github.com/aicuai/music-slide-maker/issues/1)
- ブランチ: `issue/1-ppsg123-timing-and-marp`

---

*Made with Remotion + Marp + aubio by AICU Inc.*
