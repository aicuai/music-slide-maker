# 2026-03-08 作業記録: music-slide-maker 初回ビルド

## やったこと

### 1. プロジェクト確認

`music-slide-maker`（正式名: `marp-lyric-video`）は、Marp形式のMarkdownで歌詞を書くとRemotionで リリックビデオ（MP4）を自動生成するツール。

- **技術スタック**: Remotion + Marp + React + TypeScript
- **主な機能**: Markdown → 歌詞動画変換、BGM合成、アニメーション（fade/slide/scale/typewriter）

### 2. 依存パッケージのインストール

```bash
npm install
```

- 301パッケージがインストールされた
- 脆弱性: 0件

### 3. ビルドエラーの修正

`npm run build` を実行したところ、エントリーポイントのパスが間違っていた。

**問題**: `package.json` のスクリプトが `src/index.ts` を参照していたが、ソースファイルはすべてルート直下に配置されていた。

**修正内容** (`package.json`):
- `src/index.ts` → `index.ts`
- `src/cli.ts` → `cli.ts`
- `src/generate-slides.ts` → `generate-slides.ts`
- `src/import-lyrics.ts` → `import-lyrics.ts`
- その他スクリプトも同様に修正

### 4. ビルド成功

```bash
npm run build
```

```
⚡️ Cached bundle. Subsequent renders will be faster.
+ /Users/aki/git.local/music-slide-maker/build
```

Remotionバンドルが `build/` ディレクトリに正常に生成された。

### 5. サンプル動画のレンダリング

```bash
npm run render
```

- 360フレーム（12秒 @ 30fps）をレンダリング
- Chrome Headless Shellを自動ダウンロード（初回のみ）
- 並列レンダリング: 5x concurrency
- 出力: `out/video.mp4`（1.1 MB）

レンダリング完了！サンプル歌詞からリリックビデオが正常に生成された。

### 6. Remotion Studio 起動

```bash
npm run studio
```

http://localhost:3000 でリアルタイムプレビュー環境が起動。ブラウザ上でアニメーションやタイミングを確認しながら調整できる状態に。

### 7. 歌詞・アニメーションのカスタマイズ

`sample-lyrics.md` を「月夜のメロディー」（12スライド、33秒）に書き換え。スライドごとに異なるアニメーション（fade/slide/scale/typewriter）を指定できるよう `marp-parser.ts` を拡張。

```markdown
<!-- duration: 3, animation: slide -->
君の声が 風に乗って
```

のように、1つのコメントで表示時間とアニメーション種類を同時に指定可能に。

### 8. 実楽曲でBGM付きリリックビデオ生成

**楽曲**: PicoPico! Shooting Game 123 - Mina Azure (#PPSG #AiCuty)

1. `yt-dlp` をインストールしてYouTubeからMP3を取得 → `public/ppsg123.mp3`（105秒）
2. 歌詞34スライドをタイミング付きMarkdownに変換 → `ppsg123-lyrics.md`
3. `Root.tsx` を楽曲専用に更新（ピンク系カラースキーム `#ff69b4`、ダーク背景 `#0a0014`）
4. BGM付きでレンダリング

```bash
npm run render
```

- 3060フレーム（102秒 @ 30fps）
- 出力: `out/video.mp4`（7.5 MB）
- BGM付きリリックビデオが完成！

### 9. 楽曲分析によるタイミング同期

Issue [#1](https://github.com/aicuai/music-slide-maker/issues/1) を起票してブランチ `issue/1-ppsg123-timing-and-marp` で作業。

**音声分析ツール**:
- `aubio` をインストールしてビート検出
- 結果: **BPM 121**、1拍=0.496秒、1小節=1.983秒、最初のビート=1.606秒

**タイミング同期手法**:
1. aubio beat detection で188個のビートポイントを検出
2. YouTube自動字幕（`ja-orig`）の22エントリからキーポイントを取得
3. ビートと字幕を照合して小節番号にマッピング
4. 小節単位（`fromBar(n)`関数）で全34フレーズを配置

**スタイル変更**:
- 位置: 画面中央下部（`alignItems: flex-end` + `paddingBottom: 8%`）
- 文字色: 赤（`#ff1a1a`）
- フチ: 白い縁取り（`WebkitTextStroke: 3px white`）
- フォント: ゴシック体（`Hiragino Kaku Gothic ProN`）

**バグ修正**: 短いスライド（8フレーム等）でフェードイン/アウトが重複してRemotionがクラッシュする問題を修正。

### 10. Marpスライド（PowerPoint形式）生成

`ppsg123-slides.md` を作成し、Marp CLIで以下を生成:

- **PPTX**: `out/ppsg123-slides.pptx`（5.7 MB）— PowerPointで開ける
- **HTML**: `out/ppsg123-slides.html`（123 KB）— ブラウザでプレゼン

16スライド構成:
1. タイトル（ネオン風）
2. 楽曲情報テーブル
3-14. 歌詞（Verse/Chorus/Bridge セクション別デザイン）
15. エンディング
16. 制作情報

## まとめ

初回ビルドのパス修正から始まり、歌詞カスタマイズ、パーサー拡張、実楽曲でのBGM付きリリックビデオ生成、楽曲分析によるタイミング同期、Marpスライド生成まで完了。

**成果物**:
- `out/video.mp4` — BGM付きリリックビデオ（7.8 MB / 105秒）
- `out/ppsg123-slides.pptx` — PowerPointプレゼン（16スライド）
- `out/ppsg123-slides.html` — HTMLプレゼン
